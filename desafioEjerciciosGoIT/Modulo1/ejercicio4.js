/**
 * Complete el código asignando, 
 * de la expresión de la variable totalPrice para calcular el 
 * importe total del pedido.
 * La variable pricePerItem almacena el precio de un artículo y 
 * orderedQuantity almacena el número de artículos del pedido.

Declare la variable pricePerItem
Declare el valor pricePerItem del número 3500
Declare la variable orderedQuantity
Declare el valor orderedQuantity del número 4
Declare la variable totalPrice
Declare el valor totalPrice del número 14000
Operador empleado *
 */
const pricePerItem = 3500;
const orderedQuantity = 4;
const totalPrice = pricePerItem * orderedQuantity;

console.log(totalPrice);