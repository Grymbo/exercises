/**
 * Declare dos variables:

Nombre de la variable	Valor esperado
lastElementIndex	El índice del último elemento del array fruits a través de longitud_del_array- 1
lastElement	El valor del último elemento del array
Declare la variable lastElementIndex
El valor de la variable lastElementIndex del número 3
Declare la variable lastElement
El valor de la variable lastElement del número "banana"
 */

const fruits = ["apple", "plum", "pear", "orange"];

const lastElementIndex = fruits.length - 1;
const lastElement = fruits[fruits.length -1];

console.log(lastElementIndex);
console.log(lastElement);